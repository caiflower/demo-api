apiVersion: v1
kind: Service
metadata:
  name: demo-api-service
  namespace: demo-api
spec:
  selector:
    app: demo-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP